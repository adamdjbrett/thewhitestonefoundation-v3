---
layout: base.njk
title: "Our Contributors"
description: "Meet the amazing authors who contribute to our content."
permalink: /authors/
---

<section class="container py-5">
  <div class="text-center mb-5">
    <h1 class="serif display-4">{{ title }}</h1>
    <p class="lead text-muted">{{ description }}</p>
  </div>

  <div class="row g-4 justify-content-center">
    {% for author in collections.authors %}
    <div class="col-md-4 col-lg-3 text-center">
      <a href="{{ author.url }}" class="text-decoration-none text-dark">
        <div class="author-avatar mb-3 mx-auto shadow-sm" style="width: 150px; height: 150px; border-radius: 50%; overflow: hidden;">
          <img src="{{ author.data.image or metadata.image }}" class="w-100 h-100" style="object-fit: cover;">
        </div>
        <h2 class="h5 fw-bold mb-1">{{ author.data.name }}</h2>
        <p class="small text-muted text-uppercase tracking-widest">{{ author.data.role }}</p>
      </a>
    </div>
    {% endfor %}
  </div>
</section>